const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			colors: {
				primary: '#150050',
				secondary: '#000000',
				accent: '#610094'
			}
		}
	},

	plugins: []

	//darkMode: 'class',
};

module.exports = config;
